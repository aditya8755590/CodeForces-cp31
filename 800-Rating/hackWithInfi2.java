// You are given an array A of N integers and Q queries. Each query is either:
// • Update (i, x): Set Al] = x.
// wariya
// • Query(L, R): Find maximum value M in [L, R]. Compute the Recaman
// distance: starting from M, how many steps until the recaman sequence
// revisits a value or exceeds 10^6? (Recaman: RO) =0, R(n) =R(n-1)-n if
// positive and unvisited, else R(n-1) +n). Return steps x multiplier, where
// multiplier = 17 if M is a fortunate number (smallest m > 1 where p#+m i s
// prime, for some primorial p#), else 1.
// Find the sum of all query results.
public class hackWithInfi2 {
    
}
